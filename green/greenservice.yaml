apiVersion: apps/v1
kind: Service
metadata:
  name: capstone-bg-lb
  labels:
    app: capstone-bg-lb
spec:
  selector:
    app: green
  ports:
  - port: 8000
    targetPort: 80
    protocol: TCP
  type: LoadBalancer

